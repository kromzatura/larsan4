import { describe, it, expect } from 'vitest';

// Placeholder only. Real SSR tests are in compare-ssr.test.tsx.
describe('placeholder', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
